jQuery(document).ready(function($) {
	$( '#it-exchange-group-membership-restriction' ).on( 'click', function( event ) {
		$( '.it-exchange-buddypress-group-memberships' ).toggleClass( 'hidden' );
	});
});